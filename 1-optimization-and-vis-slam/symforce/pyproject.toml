[project]
name = "dummy_project"
version = "0.1.0"
description = "Template project"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "requests",
    "numpy",
    "ipykernel",
    "symforce>=0.10",
]
