[project]
name = "SymforceProject"
version = "0.1.0"
description = "Symforce project"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "requests",
    "numpy",
    "ipykernel",
    "symforce>=0.10",
]
